# 实验实施与 AI 分工计划

> 项目：基于双轴分诊的人机协同决策界面部署前评估（v2.4）
> 场景：单人负责全部工作 + Copilot Premium（多模型、token 不限额），拆多个 agent / session 并行代工
> 本文回答一个问题：**这件事能不能"尽量全部让 AI 做"？可行性到底在哪。**

---

## 0. 结论（可行性判断）

**可行。代码量的约 80–90% 可以交给 AI 写。** 这个项目的主体是"数据管道 + agent panel 编排 + 统计计算 + 出图出表"，全是 AI 的强项，且模块边界清晰、适合拆 session 并行。

**但有三道闸是 AI 过不了的，必须你先亲自清掉——它们决定项目能不能启动，与写多少代码无关：**

| 闸 | 内容 | 不过的后果 |
|---|---|---|
| **闸 1 · 数据** | 你能否真正拿到 Bansal CHI'21 与 Lu & Yin CHI'21 的**原始 trial 级数据**（不是论文里的图表，是可下载的 CSV/日志） | 拿不到 → 整个 E1 无法校准、无法验证，项目空转 |
| **闸 2 · 批量推理通道** | "Copilot Premium 不限额"是否等于**可编程的批量 API**（能在 Python 里循环调几万次） | 只有交互聊天 → agent panel 的核心推理循环跑不起来 |
| **闸 3 · 你自己当 PI** | 科学正确性（无数据泄漏、无污染、统计不被自己骗）无法外包 | 全托管 AI → 会产出"能跑、数字漂亮、但方法学是错的"管道，对一篇**方法论**论文是致命的 |

**一句话：AI 能当你的整个工程团队，但当不了你的 PI。** 你的角色从"写代码的人"变成"定架构 + 做集成 + 验正确性 + 解释结果"的人——这恰好是 AI 干不了的高价值 10%。

---

## 1. 三道闸，逐个说清

### 闸 1 · 数据（第一周必须清掉，先于一切代码）
- Bansal 和 Lu & Yin 是 2021 CHI 论文。**它们的原始 per-trial 数据不一定公开可下**——有的作者放 OSF/GitHub，有的要邮件申请，有的根本没放。
- **AI 无法可靠地帮你搞到 gated 数据**（会一本正经地编出一个不存在的下载链接）。
- **动作**：第一周去核实这两个数据集的可获取性；拿不到就邮件问作者，或立刻把"§7 建议补的 2024–2026 现代 reliance 数据集"从"可选"提为"必需"，甚至考虑用 E6 的小规模自采来当种子数据。**这一步没结论之前，别让 AI 开始写数据管道**——否则它会对着一个想象中的数据格式写一堆白工。

### 闸 2 · 批量推理通道（决定 agent panel 跑不跑得起来）
- E1 的规模是 **persona × 任务 × UI 变体 × seed × 模型家族**，轻松上万次调用。
- **"Copilot Premium 不限额" ≠ 可编程批量 API。** GitHub Copilot / M365 Copilot 是**交互式助手**，不一定给你一个能在 `for` 循环里调几万次的后端接口；交互式界面还会限速，跑不动批量。
- **你要核实的具体问题**：我的额度里，有没有一个**能在 Python 脚本里 loop 调用**的 endpoint（Azure OpenAI / OpenAI / Anthropic API key，或 GitHub Models 之类）？如果只有聊天窗口 → 核心实验的推理循环无法自动化。
- **兜底方案**：(a) 申请 Azure OpenAI / 各家 API key（哪怕自费，MVP 量级成本可控）；(b) 跨模型三角里的"开源权重档"用本地/托管 vLLM 跑（需要 GPU 或托管推理端点——AI 能写代码，但**开不出 GPU**）。

### 闸 3 · 你必须亲自当 PI（这是"尽量全让 AI 做"的硬边界）
- 在**产品**里，"能跑就行"是合格标准，AI 全托管没问题。
- 在**研究**里，合格标准是"**方法学正确**"。而 AI 会非常自信地产出**能跑、数字干净、但悄悄错**的管道：训练/测试泄漏、污染没隔离、null model 设错、beta-binomial 写错、researcher DoF 没记录（事后偷偷调阈值）。
- 你这篇论文**唯一的卖点就是方法学严谨**。一个静默的统计错误 = 整篇作废，而且审稿人一眼看穿。
- **所以第 6 节那份"绝不能盲信 AI"的清单，是本计划最重要的一页。**

---

## 2. AI 能做 vs 你必须亲自做

| 环节 | 谁做 | 说明 |
|---|---|---|
| 仓库脚手架、配置、日志、CLI | **AI** | 纯工程 |
| 数据加载、清洗、统一 schema | **AI**（你先给样例 + 字段定义） | 依赖闸 1 |
| 原子特征抽取（§4.3 特征向量） | **AI 起草 + 你定义特征** | 哪些认知-交互特征、怎么打分，是**科学判断**，你定；AI 实现 |
| agent panel 引擎（双系统、persona、反事实配对、摩擦代理、模型 provider 抽象） | **AI** | 依赖闸 2 |
| 统计与指标（beta-binomial 超弥散、bootstrap、置换检验、双轴信号、PAS/ECS/ECE、四条基线） | **AI 写 + 你逐行验** | **最高验证优先级** |
| 校准与双阈协议、弃权逻辑、泛化梯度 | **AI 写 + 你定判据** | 阈值、FP/FN 取向是你的决定 |
| 实验编排、出表出图、预注册冻结 | **AI** | |
| **数据获取、批量 API 通道、GPU** | **你（AI 帮不上）** | 三道闸 |
| **架构决策、集成、结果解释、"我是不是骗了自己"的判断** | **你** | 不可外包 |
| IRB / E6 前瞻真人研究 | **你** | ◆ 冲刺层，人类专属 |

**净估计**：代码体量 AI ≈ 85%；但科学责任 100% 是你的。

---

## 3. 多 agent / session 协作的头号风险：上下文碎片化

你拆多个 agent/session，最大的坑不是"AI 写不好某个模块"，而是**多个无记忆的 session 各写各的，代码拼不到一起**（接口对不上、schema 各定各的、重复造轮子）。

**解法——建一个"单一事实源"，每个新 session 第一件事是读它：**

在仓库根目录维护三份文件，当作所有 agent 的共享大脑（这正是弥补 session 无记忆的手段）：

- **`SPEC.md`** — 冻结的设计：双轴定义、RQ/H、E1–E6、双阈协议。基本就是 v2.4 开题稿的技术版。
- **`INTERFACES.md`** — 所有模块的数据 schema 与函数签名（数据帧列名、`AgentResponse` 结构、指标函数输入输出）。**这是防止代码拼不拢的关键。**
- **`PROGRESS.md`** — 已完成/进行中/待办 + 已知坑。每个 session 结束前更新它。

> **每个新 agent/session 的开场白模板**："先读 `SPEC.md` / `INTERFACES.md` / `PROGRESS.md`，严格按 `INTERFACES.md` 的签名实现 X 模块，不要改动其它模块的接口，完成后更新 `PROGRESS.md`。"

配套：**一个 Git 仓库**（单一代码源）、**集成测试**（模块拼接处有测试）、**固定随机种子 + run manifest**（每次跑记录配置，为预注册和复现服务）。

---

## 4. 模块分解与 agent 分工

清晰的模块边界 = 干净的 session 分配。依赖关系已标注。

| Agent/Session | 模块 | 依赖 | 阻塞于 |
|---|---|---|---|
| **S0（你 + AI）** | 架构：仓库、`SPEC/INTERFACES/PROGRESS`、config、schema | — | 最先做 |
| **A · 数据与特征** | 加载 Bansal/Lu&Yin → 统一 schema；抽取原子认知-交互特征 | S0 | **闸 1（数据）** |
| **B · Panel 引擎** | 双系统架构、persona、反事实配对、摩擦代理、模型 provider 抽象、序列有状态模式 | S0 | **闸 2（API）** |
| **C · 指标与统计** | beta-binomial 超弥散、bootstrap、置换检验、双轴信号、PAS/ECS/ECE、四基线 | A 的 schema | — |
| **D · 校准与协议** | 特征空间/分区校准、双阈值学习、弃权逻辑、泛化梯度、逆转区 | B + C | — |
| **E · 实验与报告** | E1–E5 编排、结果表、图、预注册日志 | A+B+C+D | — |

**并行建议**：A 和 B 可同时起步（分别阻塞于各自的外部资源）；C 只需 A 的 schema 就能开工（可用假数据先写好、真数据到位再接）。

---

## 5. 推进策略：先纵切一刀，再横向铺开（强烈建议）

**不要**让五个 agent 各把自己的模块写"完整"再集成——那对单人协调多 session 是集成地狱。

**改为：先打通一条最薄的端到端纵切（thin vertical slice）：**

> **纵切 v0**：Bansal 一个数据集 × 一个模型 × 十来个任务 × 一个 UI 对比 → 只算**轴一超弥散信号 E1** → 跑出一个"分歧度 vs 人类超弥散"的相关数字。

打通这一条，证明"数据→panel→指标→结果"整条链能拼起来、能出数。**然后再横向加**：加模型（跨家族三角）→ 加轴二（暗黑/系统性过度依赖）→ 加特征与校准 → 加 E3/E4/E5。

好处：集成风险在第一天就暴露并解决；每加一块都是在一条能跑的链上增量，而不是最后一次性大集成。

---

## 6. 你绝不能盲信 AI 的地方（验证清单 · 本计划最重要一页）

这些地方 AI 会给你"能跑但错"的代码。**每一条你都要亲自核对，或让第二个 AI 以对抗视角复核（"请找出这段统计代码里的方法学错误"）。**

- [ ] **训练/测试泄漏**：LOIO（E3）里，被留出的那一整类干预，其任何信息（包括校准阈值、特征归一化参数）**绝不能**在训练侧见过。AI 极易把归一化/标准化 fit 在全量数据上——这就是泄漏。
- [ ] **污染隔离**：验证是否真的跑在扰动版数据上；模型训练截止日期与数据发布日期的关系是否如实报告。
- [ ] **beta-binomial 超弥散**：是否真的从二项抽样噪声里**分离**出了 between-user 超弥散，还是错算成了原始方差？（这是打掉"伯努利平庸化"的命根，错了整个 H1a 塌。）
- [ ] **均值预测器基线**：它必须是"只输出 p(1−p)"的基线，且你的分歧度**确实显著优于它**——这是必须打败的对手，不能偷偷放水。
- [ ] **null model / 适当依赖锚点**：理性贝叶斯 null model 设定是否正确，"过度依赖"是否相对它定义。
- [ ] **难度控制**：E1 主估计量是否真的是 within-task 差分（难度在配对内抵消），还是 AI 图省事写成了 pooled 相关（会被难度混淆）。
- [ ] **双轴不串味**：轴一（离散）与轴二（水平）是否**分别**计算、分别设阈；暗黑条件下轴二是否确实触发（这是闭合盲区的验收点）。
- [ ] **researcher DoF / 预注册**：阈值 τ_disp、τ_level 是否**在看结果前**锁定并写进 `PROGRESS.md`？事后调参数再回填 = 科研不端。
- [ ] **多重比较**：扫多个干预时，Benjamini–Hochberg 校正是否真的做了。
- [ ] **AI 幻觉数字**：任何"结果摘要"都以**代码重跑的原始输出**为准，不信 AI 在对话里复述的数字。

> 经验法则：**能跑 ≠ 正确。** 对一篇卖方法学的论文，"正确"由你负责，AI 只负责"能跑"。

---

## 7. 风险登记

| 风险 | 概率 | 影响 | 缓解 |
|---|---|---|---|
| 拿不到原始数据（闸 1） | 中–高 | 致命 | 第一周核实；备选现代数据集 / E6 自采种子 |
| 无批量 API 通道（闸 2） | 中 | 致命 | 核实额度；备选 API key / 本地开源权重 + GPU |
| 多 session 代码拼不拢 | 高 | 高 | `INTERFACES.md` + 集成测试 + 先纵切 |
| AI 静默方法学错误 | 高 | 致命 | 第 6 节清单 + 对抗式二次复核 |
| 无限套娃打磨、迟迟不动手 | 中 | 高 | v2.4 已够；停止改稿，开跑纵切 v0 |
| 跨模型推理成本/限速 | 中 | 中 | MVP 先单模型；三角在阶段二再加 |

---

## 8. 第一周行动清单

1. **[闸 1]** 核实 Bansal 与 Lu & Yin 原始 per-trial 数据可否获取（下载 / 邮件作者 / 备选数据）。**没结论前不写数据管道。**
2. **[闸 2]** 核实你的额度是否含**可编程批量 API**；没有就搞定 API key 或本地开源推理路径。
3. **[S0]** 建 Git 仓库；用 AI 起草 `SPEC.md`（= v2.4 技术版）、`INTERFACES.md`（schema + 签名）、`PROGRESS.md`。
4. **[纵切 v0]** 派 Agent A/B/C，用**假数据**先打通"数据→panel→轴一超弥散指标"的端到端骨架（真数据到位即替换）。
5. **[验证]** 把第 6 节清单存进仓库，作为每个 PR 的 checklist。
6. **[与老师]** 拿 v2.4 敲定 venue + 是否投 E6；把闸 1/闸 2 的核实结果同步给老师（数据与算力是最该早暴露的风险）。

---

**底线**：技术上完全可行，AI 可以当你的整个工程团队。但请把自己定位成 **PI + 集成负责人 + 首席验证官**，而不是"把活全丢出去的人"。这个项目的成败，最终压在你亲自守住的那 10%——数据、算力、和方法学正确性——上面。
